spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/myplayground
    username: admin
    password:
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: validate
  batch:
    jdbc:
      initialize-schema: always